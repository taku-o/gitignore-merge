master..現在のブランチ間のコード変更が、仕様書（要件定義書・設計書・タスクリスト）の設計・構想通りに実装されているかを、専門家の視点で念入りに調査してください。

## パラメータの解決

- **feature名**（$ARGUMENTS）: 対象のfeature名

### パラメータが指定されていない場合の処理

$ARGUMENTS（feature名）が空の場合：
1. Claude Codeのメモリ（memory配下のファイル群）を確認し、直前まで作業していたfeature名を特定してください。
2. 特定できない場合は、「直前まで作業していたfeatureを特定できませんでした。feature名を指定して再実行してください。」とエラーメッセージを表示して処理を中止してください。

解決したパラメータをユーザーに表示してから作業を開始してください（例：「feature: xxx を対象にします」）。

以降、解決したfeature名を {feature} として使用します。

---

対象フィーチャー: {feature}

## 実行手順

### Step 1: 対象ファイルの事前読み込み

以下のファイルを自分で読み込んでください（存在しないファイルはその旨を報告して処理を中止すること）：
- `.kiro/specs/{feature}/requirements.md`
- `.kiro/specs/{feature}/design.md`
- `.kiro/specs/{feature}/tasks.md`

また、steeringコンテキストも全て読み込んでください：
- `.kiro/steering/` 配下の全ファイル（product.md, tech.md, structure.md, カスタムファイル含む）

### Step 2: git diffの取得

以下のコマンドを実行して、master..現在のブランチ間の変更を取得してください：

```bash
git diff master...HEAD
```

diffが非常に大きい場合は、ファイル一覧（`git diff master...HEAD --name-status`）も取得し、変更の全体像を把握してください。

また、コミット履歴も確認してください：

```bash
git log master..HEAD --oneline
```

### Step 3: 並列レビューの実行

Agent toolを使って、以下の5つのエージェントを**全て同時に（1つのメッセージで）**起動してください。
各エージェントのプロンプトには、Step 1で読み込んだ仕様書の内容と、Step 2で取得したgit diff/コミット履歴を全て含めてください。
エージェントはファイルを読むだけで、編集は行いません。

---

#### Agent 1: 要件充足レビュー（subagent_type: "general-purpose"）

description: "Review requirements fulfillment"

プロンプトに含める内容：
- requirements.md の全文
- tasks.md の全文
- git diff の全文
- git log の全文
- steering配下の全ファイルの全文

指示内容：

```
あなたはソフトウェア品質保証の専門家です。実装がrequirements.mdの全要件を満たしているかを徹底的にレビューしてください。

このレビューは時間をかけて念入りに行ってください。疑わしい箇所は実際にコードを読んで確認してください。

【精査観点】

**要件の充足性**
- requirements.md の各受け入れ基準（Acceptance Criteria）について、対応する実装がgit diffに存在するか1つずつ確認すること
- 各受け入れ基準のEARSパターン（When/While/If/Where/The [system] shall）で記述された条件・トリガー・アクションが、コード上で正しく実装されているか
- 受け入れ基準に記載された全てのケース（正常系・異常系・境界条件）が実装されているか

**実装の網羅性**
- 要件に記載されたエラーハンドリングが実装されているか
- 要件に記載されたバリデーションが実装されているか
- 要件に記載されたユーザーフィードバック（メッセージ、表示、通知等）が実装されているか

**未実装の検出**
- 一部のみ実装されている要件がないか（部分実装の検出）
- TODO、FIXME、HACK等のコメントで先送りにされている要件がないか
- モック、スタブ、ダミー値で仮実装されている箇所がないか

疑わしい箇所がある場合は、git diffだけでなく、Grep/Readツールを使って実際のソースコードを直接確認してください。推測で判断せず、コードの実物を見て判断すること。

【出力フォーマット】

以下の形式で日本語で結果を出力してください：

### 要件充足レビュー

**要件ごとの充足状況**

| 要件ID | 要件概要 | 充足状況 | 備考 |
|--------|----------|----------|------|
| X.X | （概要） | ✅充足 / ⚠️部分的 / ❌未実装 | （詳細） |

**未実装・部分実装の詳細**
（該当する要件について、何が不足しているか具体的に記述。なければ「なし」）

**仮実装・先送りの検出**
（TODO/FIXME/モック等の検出結果。なければ「なし」）

### 問題数サマリー
- 未実装の要件: X件
- 部分実装の要件: X件
- 仮実装・先送り: X件
```

---

#### Agent 2: 設計適合レビュー（subagent_type: "general-purpose"）

description: "Review design conformance"

プロンプトに含める内容：
- design.md の全文
- requirements.md の全文
- git diff の全文
- git log の全文
- steering配下の全ファイルの全文

指示内容：

```
あなたはソフトウェアアーキテクトの専門家です。実装がdesign.mdの設計に忠実に従っているかを徹底的にレビューしてください。

このレビューは時間をかけて念入りに行ってください。疑わしい箇所は実際にコードを読んで確認してください。

【精査観点】

**アーキテクチャ適合性**
- design.mdで定義されたアーキテクチャパターンに従っているか
- コンポーネント構成がdesign.mdの設計と一致しているか（余計なコンポーネントが追加されていないか、必要なコンポーネントが欠落していないか）
- レイヤー構造・依存方向がdesign.mdの設計に従っているか
- design.mdで定義されたモジュール境界が守られているか

**インターフェース適合性**
- design.mdで定義されたインターフェース（API、コンポーネントProps、関数シグネチャ等）が設計通りに実装されているか
- データフローがdesign.mdで設計された通りに流れているか
- design.mdで定義された型・データ構造が設計通りに実装されているか

**設計判断の逸脱検出**
- design.mdに記載されていない独自の設計判断が実装に含まれていないか
- design.mdのGoals/Non-Goalsに反する実装がないか
- design.mdで定義されたエラーハンドリング戦略に従っているか
- design.mdで定義されたテスト戦略に従ったテストが実装されているか

**命名・構造の一貫性**
- design.mdで定義された命名規則がコード上で守られているか
- ファイル・ディレクトリ構成がdesign.mdの構造設計と一致しているか
- steering（tech.md, structure.md）で定義されたプロジェクト規約と整合しているか

疑わしい箇所がある場合は、git diffだけでなく、Grep/Readツールを使って実際のソースコードを直接確認してください。推測で判断せず、コードの実物を見て判断すること。

【出力フォーマット】

以下の形式で日本語で結果を出力してください：

### 設計適合レビュー

**アーキテクチャ適合性**
- 適合している点：（列挙）
- 逸脱している点：（列挙。なければ「なし」）

**インターフェース適合性**
- 適合している点：（列挙）
- 逸脱している点：（列挙。なければ「なし」）

**設計判断の逸脱**
（逸脱があれば詳細に記述。なければ「なし」）

**命名・構造の一貫性**
（問題があれば列挙。なければ「問題なし」）

### 問題数サマリー
- アーキテクチャ逸脱: X件
- インターフェース逸脱: X件
- 設計判断の逸脱: X件
- 命名・構造の問題: X件
```

---

#### Agent 3: タスク完了状況レビュー（subagent_type: "general-purpose"）

description: "Review task completion"

プロンプトに含める内容：
- tasks.md の全文
- requirements.md の全文
- design.md の全文
- git diff の全文
- git log の全文

指示内容：

```
あなたはプロジェクト管理の専門家です。tasks.mdに記載された全タスクが実装で完了しているかを徹底的にレビューしてください。

このレビューは時間をかけて念入りに行ってください。疑わしい箇所は実際にコードを読んで確認してください。

【精査観点】

**タスク完了状況の確認**
- tasks.mdの各メジャータスク・サブタスクについて、対応する実装がgit diffに存在するかを1つずつ確認すること
- 各サブタスクの記述内容と実際の実装内容が一致しているか
- タスクに紐づくRequirements IDの要件が実装で満たされているか

**タスクの実装品質**
- 各タスクが「完了」と言える品質で実装されているか（中途半端な実装でないか）
- タスクの記述に含まれる全ての条件・仕様が実装に反映されているか
- 統合タスク（他タスクの結合）が適切に実施されているか

**未着手・未完了の検出**
- git diffに対応する変更が見つからないタスクがないか
- 一部のみ実装されているタスクがないか
- テストに関するタスクが実装されているか（テストコードの存在確認）

疑わしい箇所がある場合は、git diffだけでなく、Grep/Readツールを使って実際のソースコードを直接確認してください。推測で判断せず、コードの実物を見て判断すること。

【出力フォーマット】

以下の形式で日本語で結果を出力してください：

### タスク完了状況レビュー

**タスクごとの完了状況**

| タスク | 内容 | 完了状況 | 備考 |
|--------|------|----------|------|
| X.X | （内容） | ✅実装済 / ⚠️部分的 / ❌未着手 | （詳細） |

**未完了タスクの詳細**
（該当するタスクについて、何が不足しているか具体的に記述。なければ「なし」）

### 問題数サマリー
- 未着手タスク: X件
- 部分実装タスク: X件
- テスト未実装: X件
```

---

#### Agent 4: 実装妥当性レビュー（subagent_type: "general-purpose"）

description: "Review implementation quality"

プロンプトに含める内容：
- requirements.md の全文
- design.md の全文
- git diff の全文
- git log の全文
- steering配下の全ファイルの全文

指示内容：

```
あなたはシニアソフトウェアエンジニアの専門家です。実装の技術的妥当性を、仕様書の意図を踏まえて徹底的にレビューしてください。

このレビューは時間をかけて念入りに行ってください。疑わしい箇所は実際にコードを読んで確認してください。

【精査観点】

**実装の妥当性**
- 要件を満たすための実装手段として妥当か（もっとシンプルで正しい方法がないか）
- 選択されたライブラリ・フレームワークの使い方が正しいか
- エラーハンドリングが適切か（エラーを握りつぶしていないか、適切にユーザーに伝えているか）
- 入力バリデーションが適切か（セキュリティ上の懸念がないか）
- 状態管理が適切か（不要な複雑さがないか、競合状態のリスクがないか）

**コード品質**
- 関数・コンポーネントの責務が明確か（単一責任原則に従っているか）
- 可読性が十分か（変数名・関数名が意図を表しているか）
- 重複コードが不必要に存在していないか
- 明らかなバグやロジックエラーがないか
- 型安全性が確保されているか（any型の乱用、型アサーションの不適切な使用等）

**セキュリティ**
- XSS、SQLインジェクション、コマンドインジェクション等の脆弱性がないか
- 認証・認可に関する実装が適切か
- 機密情報（パスワード、トークン、API キー等）がハードコードされていないか
- ユーザー入力の適切なサニタイズが行われているか

**パフォーマンス上の明らかな問題**
- N+1クエリ等の明らかなパフォーマンス問題がないか
- 不要な再レンダリングを引き起こす実装がないか
- メモリリークの原因となる実装がないか

疑わしい箇所がある場合は、git diffだけでなく、Grep/Readツールを使って実際のソースコードを直接確認してください。推測で判断せず、コードの実物を見て判断すること。

【出力フォーマット】

以下の形式で日本語で結果を出力してください：

### 実装妥当性レビュー

**重大な問題（ブロッカー）**
（問題があれば列挙。なければ「なし」）

**中程度の問題（修正推奨）**
（問題があれば列挙。なければ「なし」）

**軽微な問題（改善提案）**
（問題があれば列挙。なければ「なし」）

**良い実装**
（特に良い実装判断があれば1-3点挙げる）

### 問題数サマリー
- 重大な問題: X件
- 中程度の問題: X件
- 軽微な問題: X件
```

---

#### Agent 5: スコープ逸脱・想定外変更の検出（subagent_type: "general-purpose"）

description: "Detect scope deviation"

プロンプトに含める内容：
- requirements.md の全文
- design.md の全文
- tasks.md の全文
- git diff の全文
- git log の全文
- steering配下の全ファイルの全文

指示内容：

```
あなたはプロジェクトマネージャーの専門家です。実装が仕様書のスコープを逸脱していないか、当初の想定と違うことをしていないかを徹底的に調査してください。

このレビューは時間をかけて念入りに行ってください。疑わしい箇所は実際にコードを読んで確認してください。

【精査観点】

**スコープ逸脱の検出**
- requirements.md / design.md / tasks.md に記載されていない機能が実装されていないか
- design.mdのNon-Goalsに記載された項目が実装されていないか
- 仕様書に記載のない「便利機能」「改善」「最適化」が追加されていないか
- 既存コードのリファクタリングが仕様書の範囲を超えていないか

**想定外の変更の検出**
- 仕様書に関係のないファイルが変更されていないか
- 既存の動作を意図せず変更している箇所がないか（回帰リスク）
- 仕様書で定義されたスコープ外のコンポーネントに影響を与えていないか
- 設定ファイル、依存関係（package.json等）に想定外の変更がないか

**実装方針の逸脱**
- design.mdで選択されたアプローチと異なるアプローチで実装されている箇所がないか
- design.mdで「使わない」と判断された技術やパターンが使われていないか
- steering（tech.md, structure.md）のプロジェクト規約に反する実装がないか

**変更の副作用分析**
- 変更されたファイルの一覧を仕様書と照合し、想定外のファイル変更がないか確認
- 既存テストに影響を与える可能性のある変更がないか
- 共有ユーティリティやグローバル設定への変更が他機能に影響しないか

疑わしい箇所がある場合は、git diffだけでなく、Grep/Readツールを使って実際のソースコードを直接確認してください。推測で判断せず、コードの実物を見て判断すること。

【出力フォーマット】

以下の形式で日本語で結果を出力してください：

### スコープ逸脱・想定外変更の検出

**スコープ逸脱**
（検出された場合は詳細に記述。なければ「なし」）

**想定外の変更**

| # | 変更ファイル | 変更内容 | 仕様書との関連 | リスク |
|---|-------------|----------|---------------|--------|
| 1 | （ファイル名） | （変更内容） | ✅関連あり / ❓不明 / ❌関連なし | 高/中/低 |

（想定外の変更がなければ「なし」）

**実装方針の逸脱**
（逸脱があれば詳細に記述。なければ「なし」）

**副作用リスク**
（リスクがあれば列挙。なければ「なし」）

### 問題数サマリー
- スコープ逸脱: X件
- 想定外の変更: X件
- 実装方針の逸脱: X件
- 副作用リスク: X件
```

---

### Step 4: 結果の統合

全エージェントの結果が返ってきたら、以下の形式に統合して出力してください。
各エージェントの結果をそのまま使い、サマリーテーブルは各エージェントの問題数サマリーから集計してください。

---

## 実装レビュー結果: {feature}

### サマリー

| レビュー観点 | 問題数 | 評価 |
|-------------|--------|------|
| 要件充足 | X件 | 🔴/🟡/🟢 |
| 設計適合 | X件 | 🔴/🟡/🟢 |
| タスク完了状況 | X件 | 🔴/🟡/🟢 |
| 実装妥当性 | X件 | 🔴/🟡/🟢 |
| スコープ逸脱・想定外変更 | X件 | 🔴/🟡/🟢 |

（🔴 = 重大な問題あり / 🟡 = 中程度・軽微な問題あり / 🟢 = 問題なし）

（ここに各エージェントの結果を順番に配置）

### 総合判定

**GO / CONDITIONAL / NO-GO**

- GO: 重大な問題なし。仕様通りに実装されている
- CONDITIONAL: 軽微な問題あり。修正後に再レビュー推奨
- NO-GO: 重大な問題あり。設計・要件との乖離が大きい

理由：（2-3文で具体的に）

### 対応が必要な項目（優先度順）

重大な問題から順に、修正すべき内容を具体的に列挙してください。
問題がない場合は「対応が必要な項目はありません」と記述してください。

---

レビューが終わったら作業を止めて、ユーザーに次の指示を求めてください。
